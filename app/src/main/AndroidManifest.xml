<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotelmanagementsystem_mobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GamingRoomCat"
            android:parentActivityName=".Categories"
            android:label="@string/bar_gaming_room"/>
        <activity android:name=".BoardGameCat"
            android:parentActivityName=".Categories"
            android:label="@string/bar_board_game"/>
        <activity
            android:name=".SportsCat"
            android:parentActivityName=".Categories"
            android:label="@string/bar_sports"/>
        <activity
            android:name=".Account"
            android:theme="@style/homeTheme" />
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity android:name=".eVouchers" />
        <activity
            android:name=".Categories"
            android:label="@string/bar_categories"
            android:parentActivityName=".Homepage" />
        <activity android:name=".CheckOut" />
        <activity
            android:name=".CheckIn"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".Homepage"
            android:theme="@style/homeTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>